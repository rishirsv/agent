# Module Blueprints

Use one file per module (for example: `combined-is.md`, `qofe-summary.md`, `nwc-summary.md`).
Each module file should define:
- required inputs
- anchor blocks (from `layout_contract.yaml`)
- formula/logic intent
- validation checks
- notes/comment placement rules
