# Workflow

Router + planning gate + iteration protocol.

TODO:
- Define mode router (new deck, edit deckSpec, QA triage).
- Define planning gate (expand vs compile).
- Define run/QA/fix iteration loop.
