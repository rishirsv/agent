# Disabled Skills

Skills in this directory are kept as repo references but are not synced into the Perks plugin.

Move a skill back under `skills/` and run `scripts/sync-plugins.sh` to re-enable it.
